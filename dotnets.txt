`dotnet-coverage` .NET Tool version depends on .NET SDK version. Since this ADR needs to support both .NET 6.0 and .NET 8.0, `dotnet-coverage` only uses version: `17.13.1`, which works for both .NET 6.0 and .NET 8.0.

